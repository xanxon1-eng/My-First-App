{
  "name": "world-builder-hub",
  "private": true,
  "description": "Static RPG Development Hub",
  "scripts": {
    "dev": "npx -y serve -p 3000 .",
    "build": "mkdir -p dist && cp index.html manifest.json sw.js dist/ 2>/dev/null || true"
  }
}
