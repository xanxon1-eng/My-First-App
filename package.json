{
  "name": "world-builder-hub",
  "private": true,
  "version": "1.0.0",
  "description": "Static PWA for RPG Development Hub",
  "scripts": {
    "dev": "npx -y serve -l 3000",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r public/* dist/ || true",
    "preview": "npx -y serve -l 3000 dist"
  }
}
