{
  "name": "base-app",
  "private": true,
  "description": ".",
  "scripts": {
    "dev": "npx -y serve -p 3000 .",
    "build": "mkdir -p dist && cp index.html manifest.json app_icon_192.png app_icon_512.png dist/ 2>/dev/null && sed \"s/const CACHE_NAME = .*/const CACHE_NAME = \\\"uhu-club-$(date +%s)\\\";/\" sw.js > dist/sw.js"
  }
}
